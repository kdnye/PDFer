# PDFer

Primary project documentation has been expanded into:

- `readme.md` (product + usage overview)
- `documentation.md` (functional, operational, and security documentation)
- `architecture.md` (technical architecture and data flow)

Start here:

```bash
npm install
npm run dev
```

Then open `http://localhost:3000`.
